# ozon_api

Use this skill for Ozon Seller API Swagger discovery, endpoint lookup, schema inspection, request and response mapping, and safe planning for future API integrations. Prefer this skill when work depends on local `swagger.json` rather than live HTTP requests.
