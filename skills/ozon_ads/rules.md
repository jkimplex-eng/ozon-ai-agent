# ozon_ads rules

- Reconcile Performance and finance before optimization logic.
- Keep coverage gaps visible instead of forcing false matches.
- Separate attributable CPC data from uncovered promo costs.
